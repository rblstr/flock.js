<div class="container">
    <div class="header">
        <a href="/"><h1>flock</h1></a>
        <form action="/" method="get" id="mainForm">
            <input type="text" name="subreddits" value=""/><button type="submit"><span>Play</span></button>
        </form>
        <br>
        {{#if links}}
            <iframe id="player" class="player" type="text/html" width="640" height="360" src="{{youTubeUrl}}" frameborder="0" allowfullscreen></iframe>

            <br>

            {{#each links}}
                <ol>
                    <li>
                        <a href="{{this.url}}">{{this.title}}</a>
                        <br>
                        <span style="color:orange">{{this.ups}}</span>
                        <span> | </span>
                        <span style="color:blue">{{this.downs}}</span>
                        <a href="{{this.permalink}}">permalink</a>
                    </li>
                </ol>
            {{/each}}
        {{else}}
            <div class="info">
                <h1>Music discovery, powered by Reddit</h1>
                <p>Simply enter one or more <a href="http://www.reddit.com/r/Music/wiki/musicsubreddits" target="_blank">musical subreddit names</a> and flock will return a playlist of what's hot for your listening pleasure</p>
            </div>
        {{/if}}
        <div class="footer">
            <h2>with &lt;3 from <a href="https://twitter.com/rblstr">@rblstr</a> and <a href="https://twitter.com/rokeeffe">@rokeeffe</a></h2>
            <h4>Check out the project and submit bug reports on <a href="https://github.com/rblstr/flock.js">GitHub</a></h4>
        </div>
    </div>
</div>