doctype html
html(lang="en")
    head
        title!= "flock - subreddit playlister"
    body
        div(class="container")
            a(href="/")
                h1= "flock"
                span= "beta"
            form(action="/", method="get")
                input(type="text", name="subreddits", value=selectedSubreddits)
                button(type="submit")
                    span= "play"
            if links
                iframe(id="player", class="player", type="text/html", width="640", height="360", src=youTubeUrl, frameborder="0", allowfullscreen)
                ol
                    each link in links
                        li
                            a(href=link.url)!= link.title
                            br
                            span(style="color:orange")!= link.ups
                            span!= "|"
                            span(style="color:blue")!= link.downs
                            a(href=link.permalink)!= "permalink"